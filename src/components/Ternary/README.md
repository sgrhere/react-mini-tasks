# Ternary Operator

## Syntax + Working
* `{ condition? statement-when-true : statement-when-false }` 
* When the condition becomes true, the statement after '?' will run and when condition becomes false, the statement after ':' will run.
* Here, we also pass some component. For e.g. when login = true, show dashboard and loginpage when false.

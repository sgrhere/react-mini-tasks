# Props
Props are the values that is passed to the component while being called from the parent container component.

## How to use it?
* `<ChildComponent name="Mouse" price="150" quantity="2" title="data" .. />` - in calling component.
* `function SharedBtn({name, price, quantity}) {...}` - in child component.
